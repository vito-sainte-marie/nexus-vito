{
  "schema": "nexus-rules/1",
  "updated_at": "2026-09-07",
  "rules": [
    {
      "id": "ENV-001",
      "scope": ["development", "github", "architecture"],
      "trigger": "start_code_lot",
      "module": "all",
      "severity": "blocking",
      "rule": "Claude must work from the expected canonical HEAD of config-par-environnement; stale-main derived execution is blocked before coding.",
      "source": "ADR-0002"
    },
    {
      "id": "ENV-002",
      "scope": ["test", "security"],
      "trigger": "test_execution",
      "module": "all",
      "severity": "blocking",
      "rule": "Autonomous code/test/fix loops are allowed in NEXUS Test within the approved lot; Production access is not implied.",
      "source": "ADR-0002"
    },
    {
      "id": "ENV-003",
      "scope": ["orchestrator", "test", "availability"],
      "trigger": "optional_test_capability_unavailable",
      "module": "all",
      "severity": "advisory",
      "rule": "An optional Test capability that is unavailable must degrade explicitly instead of preventing Claude from starting; any proof that actually requires that capability remains unsatisfied and fail-closed.",
      "source": "ADR-0003"
    },
    {
      "id": "SEC-001",
      "scope": ["security", "supabase", "connector"],
      "trigger": "service_role",
      "module": "all",
      "severity": "blocking",
      "rule": "service_role is allowed only behind a trusted server or NEXUS Connector and is forbidden in repository, browser and public workflow logs.",
      "source": "BIBLE.md"
    },
    {
      "id": "SEC-002",
      "scope": ["security", "data"],
      "trigger": "client_data_access",
      "module": "all",
      "severity": "blocking",
      "rule": "The creator administers NEXUS but the client company controls use and sharing of its data; company/site context must be explicit and isolated.",
      "source": "BIBLE.md"
    },
    {
      "id": "PROD-001",
      "scope": ["production", "release"],
      "trigger": "production_promotion",
      "module": "all",
      "severity": "human_gate",
      "rule": "No application or Production change to main, production, Supabase Production or NEXUS Production without Frederic's explicit approval; the only main exception is the human-authorized orchestration control-plane defined by ADR-0003.",
      "source": "ADR-0002, ADR-0003"
    },
    {
      "id": "GOV-003",
      "scope": ["github", "orchestrator", "control_plane"],
      "trigger": "main_workflow_change",
      "module": "orchestration",
      "severity": "human_gate",
      "rule": "A main-branch workflow change is allowed only for the orchestration control-plane, with Frederic's explicit authorization, no application files, no Production access expansion and no cleartext secret.",
      "source": "ADR-0003"
    },
    {
      "id": "ARCH-001",
      "scope": ["architecture"],
      "trigger": "business_logic_change",
      "module": "all",
      "severity": "blocking",
      "rule": "A business truth has one logical owner; wrappers, CIN and UI consume it and do not rebuild a parallel calculation.",
      "source": "BIBLE.md"
    },
    {
      "id": "QA-001",
      "scope": ["qa", "regression"],
      "trigger": "executable_change",
      "module": "all",
      "severity": "blocking",
      "rule": "Claude fixes and retests failures inside the authorized lot without opening a new decision cycle; escalate only when the fix changes scope or doctrine.",
      "source": "ADR-0002"
    },
    {
      "id": "GOV-001",
      "scope": ["orchestrator", "handoff"],
      "trigger": "decision_available",
      "module": "all",
      "severity": "blocking",
      "rule": "Never wake Claude twice for the same canonical decision while the first wake is active, successful, or its outcome is unknown; verify issue #28, workflow state and canonical Handoff first.",
      "source": "REPRISE.md"
    },
    {
      "id": "GOV-004",
      "scope": ["orchestrator", "handoff", "github"],
      "trigger": "claude_wake_failed",
      "module": "orchestration",
      "severity": "blocking",
      "rule": "If the first Claude wake for a canonical decision has objectively ended unsuccessfully and the decision remains unconsumed, no request return exists, and no equivalent Claude run is active, Orchestrator may issue exactly one controlled retry for the same decision. A second failed retry must not loop; record the incident and escalate the rail failure to Orchestrator for diagnosis before any further wake.",
      "source": "REPRISE.md; Frederic arbitration 2026-09-07"
    },
    {
      "id": "GOV-002",
      "scope": ["orchestrator", "learning"],
      "trigger": "post_lot",
      "module": "all",
      "severity": "advisory",
      "rule": "After each lot, promote durable knowledge to rules/ADR/Bible and recurring incidents to automatic checks so a twice-solved problem does not require a third identical human analysis.",
      "source": "ADR-0002"
    },
    {
      "id": "CARB-001",
      "scope": ["business_rules", "carburants"],
      "trigger": "truck_completion",
      "module": "carburants-performance",
      "severity": "business",
      "rule": "Maximum truck volume is an optimization target, never an obligation; complement only when physically safe and absorbable by forecast rotation without inventing future sales.",
      "source": "CARB-004 canonical decisions"
    }
  ]
}