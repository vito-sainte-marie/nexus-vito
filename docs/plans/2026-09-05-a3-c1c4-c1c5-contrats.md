# C1c-4 et C1c-5 — mini-contrats avant modification

**05/09/2026 · contrats uniquement · aucune correction, aucun push**

## Une correction de mon estimation précédente

J'ai annoncé « ~15 fichiers de test » pour `chargerControleJour`. C'est faux, et
l'écart vient d'un détail qui change tout : **la quasi-totalité de ces fichiers
STUBBENT la fonction** —

```js
chargerControleJour: async () => ({ parCarburant: {…} })
```

Un stub ignore ses arguments. **Ajouter un paramètre ne les casse pas.**
Seuls les tests qui appellent la VRAIE fonction sont concernés : il y en a
**2**, plus **2 simulations**. Le coût réel est donc quatre fois moindre que ce
que j'ai avancé.

---

# C1c-4 · deux chaînes distinctes

Elles n'ont ni les mêmes appelants ni le même point de résolution. Les traiter
ensemble mélangerait deux ripples indépendants.

## C1c-4a — chaîne Commande carburant

### Signatures

```js
// actuel
chargerConfigEtCuves(client, siteId) → { config, cuves, fuseau, horaires }
evaluerCommandeCarburantSite(client, siteId, options)

// cible
chargerConfigEtCuves(client, siteId) → { config, cuves, horaires }   // le fuseau SORT
evaluerCommandeCarburantSite(client, siteId, options)                // options.timezone REQUIS
```

**Aucune signature positionnelle ne change.** `options` existe déjà sur tous
les appels : le fuseau y entre comme les autres données de contexte
(`dateISO`, `heureHHMM`, `maintenant`). C'est ce qui rend ce lot tenable.

### Appelants production — 4 réels

| Fichier | Ligne | Nature | Résout déjà le site ? |
|---|---|---|---|
| `NEXUS-Carburants-Pilotage-v1.html` | 1906 | **résolveur d'écran** | oui, `SITE_ID` |
| `NEXUS-Carburants-Pilotage-v1.html` | 2852 | résolveur (simulation) | oui, `SITE_ID` |
| `nexus-brief-donnees.js` | 572 | **couche de données** | reçoit `siteId` |
| `nexus-carburant-demarrage-mois-v1.js` | 140 | **amorçage d'écran** | oui, `site` |

Plus **trois enveloppes P0** — `p0-fixes.js:390`, `p0-coherence-ui.js:55`,
`p0-ui.js:99` — qui font `apply(this, arguments)` : elles transmettent
`options` sans le lire. **Elles n'ont rien à changer**, et c'est la preuve que
le passage par `options` était le bon choix.

### Où le fuseau est résolu, une seule fois

- **Pilotage** : déjà fait en C1c-2 (`FUSEAU_STATION`). Les deux appels le
  passent, aucune lecture supplémentaire.
- **`demarrage-mois`** : module d'amorçage qui possède déjà `site` — il
  résout une fois au démarrage, comme un écran.
- **`brief-donnees`** : couche de données, donc **ne résout pas**.
  `chargerCandidatCommandeCarburant(client, siteId)` devient
  `chargerCandidatCommandeCarburant(client, siteId, timezone)`. Son unique
  appelant est `NEXUS-Brief-v1.html:573` — un écran. **Le ripple s'arrête
  là**, sur un résolveur. C'est le test que la frontière tient.

### Si `timezone` manque

`evaluerCommandeCarburantSite` renvoie son propre vocabulaire d'échec, déjà
existant, plutôt qu'une exception :

```js
{ ok: false, etatGlobal: 'non_calculable',
  motif: "Fuseau du commerce non résolu — aucune recommandation de commande ne peut être datée." }
```

Raison : cette fonction **renvoie déjà** `ok:false, motif` pour une config
absente, et ses six appelants savent l'afficher. Lever ici obligerait chacun à
apprendre un second mode d'échec pour la même famille de cause.

`heureHHMMAujourdhui`, elle, lève déjà (C1c-1) : c'est une fonction pure, elle
n'a pas d'affichage à proposer.

### Tests impactés

`test_carburant_commande_donnees_v2238.js` (4 appels réels),
`_diagnostic_ecart_v2265` (3), `_fin_de_mois_livraison_v2257` (2),
`_regime_couverture_minimum_v2260` (2), `_ancre_jaugeage_v2255` (1),
`_journal_recommandation_v2259` (2) — **6 fichiers**, tous par ajout de
`timezone: 'America/Martinique'` dans un `options` déjà présent.
`_notification_v2239` stubbe : intact.

## C1c-4b — chaîne Performance / ventes

### Signatures

```js
// actuel
chargerVentesPeriode(client, siteId, debut, fin)      // fuseau résolu en interne par le patch P0
// cible
chargerVentesPeriode(client, siteId, debut, fin, timezone)
```

Particularité : le fuseau n'est pas lu dans la fonction d'origine
(`nexus-carburant-donnees.js:36`) mais dans le **monkey-patch** qui la
remplace (`nexus-carburants-p0-performance.js:84`). Le contrat doit donc
couvrir les deux, sous peine de laisser le patch redevenir un résolveur.

### Appelants production — 6

| Fichier | Ligne | Nature |
|---|---|---|
| `NEXUS-Carburants-Pilotage-v1.html` | 3603, 3751 | résolveur (`FUSEAU_STATION` déjà résolu) |
| `nexus-brief-donnees.js` | 169, 170, 341 | couche de données → reçoit `timezone` |
| `NEXUS-Rapport-v1.html` | 238 | **résolveur — pas encore traité** |

**`NEXUS-Rapport-v1.html` n'a aujourd'hui aucune résolution de fuseau.** Il
faudra l'ajouter, comme Verify et Pilotage en C1c-2. C'est une extension du
périmètre, pas un imprévu : elle appartient à ce lot.

### Si `timezone` manque

Lève. `chargerVentesPeriode` calcule un jour de référence : sans fuseau, elle
ne peut produire qu'une date fausse. Contrairement à
`evaluerCommandeCarburantSite`, elle n'a pas de vocabulaire d'échec à réutiliser.

### Tests impactés

`test_carburant_brief_fallback_verifie_v2248.js` stubbe : intact. Les tests de
`p0-performance` appellent le patch : à vérifier au moment du lot.

---

# C1c-5 · `chargerControleJour`

### Signatures

```js
// actuel
chargerControleJour(client, siteId, date)
// cible
chargerControleJour(client, siteId, date, timezone)
```

Quatrième paramètre positionnel plutôt qu'un `options` : cette fonction n'en a
pas, et lui en inventer un pour une seule donnée créerait deux conventions
dans le même module.

### Les appelants production — liste exhaustive : 5 appels + 3 enveloppes

| # | Fichier | Ligne | Nature | Résout déjà le site ? | Fuseau disponible ? |
|---|---|---|---|---|---|
| 1 | `NEXUS-Carburants-Pilotage-v1.html` | 1295 | résolveur | oui `SITE_ID` | **oui**, C1c-2 |
| 2 | `NEXUS-Rapport-v1.html` | 373 | résolveur | oui `SITE_ID` | **non — à ajouter** |
| 3 | `NEXUS-Carburants-v1.html` | 679 | résolveur | oui `siteId` | **non — à ajouter** |
| 4 | `nexus-brief-donnees.js` | 168 | couche de données | reçoit `siteId` | reçoit `timezone` |
| 5 | `nexus-carburant-commande-donnees-core.js` | 297 | couche de données | reçoit `siteId` | vient d'`options.timezone` (C1c-4a) |
| — | `nexus-carburants-p0-fixes.js` | 257 | enveloppe | — | transmet |
| — | `nexus-carburants-p0-ui.js` | 162 | enveloppe | — | transmet |
| — | `nexus-app-donnees.js` | 141 | délègue à `brief-donnees` | — | via Brief |

**Deux écrans à équiper d'une résolution** : Rapport et Carburants. Même
motif qu'en C1c-2, `await NexusStation.fuseauDeLaStation(SITE_ID)` avant tout
rendu.

**Dépendance d'ordre** : l'appelant n°5 reçoit son fuseau du contrat C1c-4a.
**C1c-4a doit donc précéder C1c-5.**

### Tests et simulations réellement impactés — 4

| Fichier | Pourquoi |
|---|---|
| `test_chaine_temporelle_carburant_20260821.js` | 3 appels réels (déjà en échec connu) |
| `test_carburant_performance_reception_20260902.js` | 1 appel réel |
| `simulations/scenarios-carburant.js` | 1 appel réel |
| `simulations/executer-carburant.js` | 1 appel réel |

Tous les autres stubbent, et un stub ignore un argument supplémentaire.

### Si `timezone` manque

Lève (`TypeError`). C'est une couche de données appelée par du code, jamais
par un utilisateur : l'absence est une erreur de contrat, pas un état à
afficher.

### Preuve qu'aucun repli ne subsiste dans le chemin d'appel

`test_fuseau_contexte_explicite_20260905.js` porte déjà la liste
`RESTE_A_TRAITER` — trois fichiers nommés, un par sous-lot ouvert. Quand
C1c-4a, C1c-4b et C1c-5 seront faits, la liste doit être **vide**, et le test
échoue aussi si un fichier en sort sans être retiré : « pour que *reste à
traiter* ne devienne pas une liste que plus personne ne lit. »

S'y ajoutera une vérification de chemin : aucune des fonctions de ces trois
chaînes ne doit contenir `station_config` + `fuseau_horaire` dans la même
requête.

---

# Ordre imposé par les dépendances

```
C1c-4a  (commande carburant)   →  C1c-4b  (performance/ventes)  →  C1c-5
```

C1c-4a d'abord parce que `commande-donnees-core` fournit le fuseau à l'appelant
n°5 de C1c-5. C1c-4b indépendant, placé avant C1c-5 pour équiper Rapport une
seule fois — il est appelant dans les deux.

**Aucune correction engagée. `main` et `production` restent à `501c0c7`.**
